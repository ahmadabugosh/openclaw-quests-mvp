[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PORT=${PORT:-3000} HOSTNAME=0.0.0.0 node .next/standalone/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
